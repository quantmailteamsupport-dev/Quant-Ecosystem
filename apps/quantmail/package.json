{
  "name": "@quant/quantmail",
  "version": "1.0.0",
  "private": true,
  "description": "QuantMail - Central authentication, email, and code management hub for the Quant Ecosystem",
  "main": "api/server.ts",
  "scripts": {
    "dev": "node --loader ts-node/esm api/server.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@quant/auth": "*",
    "@quant/ai": "*",
    "@quant/common": "*",
    "@quant/database": "*",
    "@quant/realtime": "*",
    "@quant/shared-ui": "*"
  }
}
