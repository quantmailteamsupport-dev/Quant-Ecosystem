{
  "name": "@quant/quantchat",
  "version": "1.0.0",
  "private": true,
  "description": "QuantChat - Snapchat-like messaging app with stories, snaps, calls, AR filters, and AI integration",
  "main": "api/server.ts",
  "scripts": {
    "dev": "node --loader ts-node/esm api/server.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@quant/auth": "*",
    "@quant/ai": "*",
    "@quant/common": "*",
    "@quant/database": "*",
    "@quant/realtime": "*",
    "@quant/shared-ui": "*"
  }
}
