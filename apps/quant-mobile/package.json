{
  "name": "@quant/quant-mobile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@quant/common": "workspace:*",
    "@quant/sync-engine": "workspace:*",
    "@quant/auth": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^2",
    "typescript": "~5.5.0"
  }
}
