{
  "name": "@quant/quantube",
  "version": "1.0.0",
  "private": true,
  "description": "QuantTube - Video, Music, and Shows streaming platform with live streaming and creator tools",
  "main": "api/server.ts",
  "scripts": {
    "dev": "node --loader ts-node/esm api/server.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@quant/auth": "*",
    "@quant/ai": "*",
    "@quant/common": "*",
    "@quant/database": "*",
    "@quant/realtime": "*",
    "@quant/shared-ui": "*"
  }
}
