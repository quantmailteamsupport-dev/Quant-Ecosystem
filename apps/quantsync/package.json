{
  "name": "@quant/quantsync",
  "version": "1.0.0",
  "private": true,
  "description": "QuantSync - Twitter/X + Threads + Reddit hybrid with anonymous feeds and live spaces",
  "main": "api/server.ts",
  "scripts": {
    "dev": "node --loader ts-node/esm api/server.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@quant/auth": "*",
    "@quant/ai": "*",
    "@quant/common": "*",
    "@quant/database": "*",
    "@quant/realtime": "*",
    "@quant/shared-ui": "*"
  }
}
