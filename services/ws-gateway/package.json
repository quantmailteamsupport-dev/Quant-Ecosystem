{
  "name": "@quant/ws-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "dev": "node --loader ts-node/esm --watch src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint .",
    "build": "tsc"
  },
  "dependencies": {
    "@quant/health-server": "workspace:*",
    "@quant/common": "workspace:*",
    "@quant/realtime": "workspace:*",
    "pino": "^9.6.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^2",
    "typescript": "~5.5.0"
  }
}
